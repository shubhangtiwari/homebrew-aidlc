# Minimal Tooling (reference)

Repository with governance, docs, and scripts but no application source tree yet. Used by
`init-architecture` when the repo has no domain signals.

## Layers

Not applicable until the user adopts a domain profile via `init-architecture`.

## Typical paths

`.ai/`, `docs/`, `Makefile`, optional CI configs.

## Next step

Re-run `init-architecture` after the repo gains a manifest and an initial source layout.
